// index.js
const head   = require('./head');
const tail   = require('./tail');
const middle = require('./middle');
const shortlived = require ('./shortlived');

module.exports = {
  head:   head,
  tail:   tail,
  middle: middle,
  shortlived:   shortlived
};